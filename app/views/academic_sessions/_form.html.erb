<%= form_with(model: academic_session, local: true) do |form| %>
  <% if academic_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(academic_session.errors.count, "error") %> prohibited this academic_session from being saved:</h2>

      <ul>
      <% academic_session.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :from %>
    <%= form.date_select :from %>
  </div>

  <div class="field">
    <%= form.label :to %>
    <%= form.date_select :to %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
